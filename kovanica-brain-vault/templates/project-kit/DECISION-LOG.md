# DECISION-LOG — append-only log

Why we chose what we chose. **Never delete or rewrite rows** — supersede by
appending a new row that names the old one. Prevents re-litigating settled
questions and protects intentional weirdness from being "fixed".

| Date | Decision | Why | Rejected alternatives |
|------|----------|-----|-----------------------|
| YYYY-MM-DD | <what was decided> | <the reasoning that mattered> | <options considered and dropped> |

---

*From the MARKDOWN.god doctrine: Master Planning System (*Intake contract*) — protects settled questions from re-litigation*
