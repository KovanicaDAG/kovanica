# CODE-MAP — topic map

Topic → path, so navigation is a lookup instead of a quest. Keep rows
small and specific. When code moves, the row moves in the same change.

| Topic | Path | Notes |
|-------|------|-------|
| Entry point | `src/main` | |
| Config | | |
| Data model | | |
| Auth | | |
| API layer | | |
| Hash Infrastructure | `/root/kovanica-hash` | `pool` for Blake3 native mining, `proxy` for ASIC foreign mining |

## Test map

| What it covers | Where | How to run just this |
|----------------|-------|----------------------|
| | | |

## Generated / do-not-touch

- `<path>` — generated by <tool/command>; edit sources, not this

---

*From the MARKDOWN.god doctrine: Master Planning System §2 (*Recon before plan*) — navigation is a lookup, not a quest*
