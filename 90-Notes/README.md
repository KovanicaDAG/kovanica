# Scratch space. Nothing here is read, written, or deleted by
# scripts/build-vault.py — it is excluded from the build's clean step and is
# never vendored from the repo. Safe to use for rough thinking.

# Example — delete or replace:
# [[10-Protocol/protocol--docs--TOKENOMICS]] — cross-check the emission curve
# against what /api/head actually reports before trusting either.
