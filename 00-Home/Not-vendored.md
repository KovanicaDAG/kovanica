---
title: Not vendored
category: index
synced: 2026-09-26
generated_by: scripts/build-vault.py
---

# Deliberately not vendored

Left out on purpose. Each is either infra-sensitive or regenerable.
The vault is git-backed and syncs to a phone, so operationally
sensitive material stays in the repo where it belongs.

- `.opencode/agents/cli-tui-dev.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/agents/client-wallet.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/agents/deploy-ops.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/agents/docs-writer.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/agents/explorer-web.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/agents/genesis-testnet.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/agents/htlc-defi.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/agents/kovanica-core.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/agents/nft-rwa.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/agents/node-ops.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/agents/p2p-network.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/agents/project-plan.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/agents/reviewer.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/agents/security-audit.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/agents/tokenomics.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/commands/audit.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/commands/autoskill.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/commands/bench.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/commands/check.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/commands/cli.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/commands/compact.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/commands/deploy.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/commands/env.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/commands/genesis.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/commands/head.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/commands/newrfc.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/commands/plan.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/commands/release.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/commands/review.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/commands/rfc006.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/commands/seedcheck.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/commands/supply.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/commands/txflow.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/commands/usage.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/plugins/README.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/skills/create-skill/SKILL.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/skills/kovanica-cli-ux/SKILL.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/skills/kovanica-consensus/SKILL.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/skills/kovanica-deploy/SKILL.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/skills/kovanica-node-ops/SKILL.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/skills/kovanica-p2p/SKILL.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/skills/kovanica-rfc-authoring/SKILL.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/skills/kovanica-rfc006/SKILL.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/skills/kovanica-security/SKILL.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/skills/kovanica-tx/SKILL.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/skills/kovanica-web/SKILL.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/skills/skill-forge/SKILL.md` — agent doctrine — re-run with --with-agent-config
- `.opencode/tools/README.md` — agent doctrine — re-run with --with-agent-config
- `TESTNET-RESET-CREDENTIALS.md` — infra-sensitive — re-run with --include-sensitive
- `TESTNET-RESET-PROCEDURE.md` — infra-sensitive — re-run with --include-sensitive
